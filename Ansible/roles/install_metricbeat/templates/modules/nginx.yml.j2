- module: nginx
  metricsets: ["stubstatus"]
  enabled: true
  period: 10s

  # Nginx hosts
  hosts: ["http://192.168.0.164"]

  # Path to server status. Default nginx_status
  server_status_path: "nginx_status"
